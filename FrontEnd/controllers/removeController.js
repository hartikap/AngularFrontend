main_module.controller('removeController', function($scope, friendDataFactory, $location) {
    
    
    $scope.addToDeleteClicked($event,$index,id) {
        
        // Lisää tämä id poistettavien listaan
    
    
    
}